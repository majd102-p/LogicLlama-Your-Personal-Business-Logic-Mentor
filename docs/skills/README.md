# CodeScene MCP Skills (copied)

This directory contains a preserved copy of the public skills that shipped in `.skills/`.

Each skill appears in its own folder with a `SKILL.md` file. See the original `.skills/README.md` for descriptions and usage notes.

(These files were copied into `docs/skills/` for project visibility and contributor onboarding.)
